[project]
name = "cc-stats"
version = "0.1.0"
description = "Process Claude Code session transcripts into blog stats"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
process-cc-session = "cc_stats.main:main"
